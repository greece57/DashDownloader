
public class MPGParser {
  public MPGInfos parseFile(String mpgFile)
  {
    MPGInfos info = new MPGInfos();
    
    // parse mpgFile and return info
    
    /*
     * info.setMinimumBandwidths(topQuali, highQuali, mediumQuali);
     * 
     * For each Segment Insert the URL to the 4 Quality Segments {
     *    info.insertNewSegments(topQuali, highQuali, mediumQuali, lowQuali);
     * }
     */
    
    return info;
  }
}
